# AgriMind

This folder contains the extracted source code from `agrimind_full_v3.html`, split into separate files for GitHub.

## Files

- `index.html` - page markup and external file links
- `style.css` - extracted CSS styles
- `script.js` - extracted JavaScript behavior

## Run locally

Open `index.html` in a browser, or serve the folder with any static web server.